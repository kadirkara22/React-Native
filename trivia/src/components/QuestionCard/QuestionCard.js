import React from 'react'
import { View, Text } from 'react-native'
import styles from "./QuestionCard.style"
const QuestionCard = ({ questions, ques, options }) => {
    console.log(questions[ques].question)
    return (
        <View>
            <Text>12</Text>
        </View>

    )
}

export default QuestionCard
