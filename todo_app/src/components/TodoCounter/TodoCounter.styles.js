import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {

        margin: 10,
        flexDirection: "row",
        justifyContent: "space-between"
    },
    title: {
        fontSize: 40,
        fontWeight: "bold",
        color: "#E39505"
    },
    counter: {
        fontSize: 40,
        fontWeight: "bold",
        color: "#E39505"
    }
})